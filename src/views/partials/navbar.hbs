<!-- Navbar -->
<nav class="navbar navbar-expand-lg bg-white shadow-sm sticky-top">
  <div class="container">
    <a class="navbar-brand" href="/">
      <img src="/assets/img/dumbways.png" class="align-text-bottom" alt="Bootstrap" height="24" />
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link {{#if home}} active {{/if}}" href="/">Home</a>
        </li>
        {{#if user}}
        <li class="nav-item">
          <a class="nav-link {{#if addProject}} active {{/if}}" href="/project/add">Add Project</a>
        </li>
        {{/if}}
        <li class="nav-item">
          <a class="nav-link {{#if testimonials}} active {{/if}}" href="/testimonial">Testimonials</a>
        </li>
      </ul>
      <ul class="navbar-nav mb-2 mb-lg-0">
        {{#if user}}
        <li class="nav-item me-3">
          <a class="nav-link">Welcome, {{user.name}}</a>
        </li>
        {{else}}
        <li class="nav-item">
          <a class="nav-link {{#if register}} active {{/if}}" href="/register">Register</a>
        </li>
        <li class="nav-item me-3">
          <a class="nav-link {{#if login}} active {{/if}}" href="/login">Login</a>
        </li>
        {{/if}}
      </ul>
      {{#if user}}
       <a href="/logout" class="btn btn-outline-danger rounded-pill">Logout</a>
      {{else}}
      <a href="/contact" class="btn btn-dark rounded-pill">Contact Me</a>
      {{/if}}
    </div>
  </div>
</nav>